# Briefing H1: image_operator

Estado: pendiente; **no despachar hasta satisfacer dependencias**.

HANDOFF-TRACE: Codex root -> root_imagegen (provider: OpenAI; model: heredado salvo ImageGen integrado) via native agent/tool
Modelo solicitado y modelo observado se registran por separado; self_report no equivale a recibo del proveedor.

## Contrato

- Fuente de verdad: ../contract.json, tarea H1. Si este briefing difiere, actualizar desde contrato antes de despacho.
- Dependencias: P0.
- Propietario: root_imagegen; proveedor OpenAI.
- Skills a leer al iniciar: imagegen, 3cucharadas-site.
- No estás solo en el workspace: no reviertas cambios ajenos, no edites fuera de allowlist y no hagas staging/commit/push.
- No publiques, envíes mensajes externos, instales dependencias ni exportes microdatos/identificadores/secretos.
- Si necesitas otro archivo, enviar propuesta a root antes de tocarlo; root tiene autoridad de integración.
- Todos los datos originales son de solo lectura; el prefijo @analysis remite al repo analítico según contrato, otros paths al blog.
- Para proveedores externos este briefing es **local**: root genera un packet público/relativo que excluye rutas internas y microdatos; no enviar este paquete entero.

## Entradas

- teasers and dedicated heroes
- existing visual catalogs

## Responsabilidad

Root usa image_gen integrado tras leer skill e inspeccionar referencias. No API pagada/CLI alternativo automático. Reusar imagen hero buena aunque herramienta exacta desconocida: provenance.status unknown documentado, no generarla para fabricar trazabilidad. Generar por deficiencia visual/familia desde teaser, una solicitud por tema y máximo una corrección dirigida; si aún falla estado partial y continuar otros temas. No 20 generaciones: una por par. Derivados deterministas explícitamente autorizados de resize/crop/encode, referencia 1600x900 1280x720 640x360 1200x630; origen inmutable y copia versionada, no borrar antiguos. Derivaciones .py solo tamaño/codificación, composición por ImageGen. Tope bytes 250000/180000/70000/180000 respectivamente; no sacrificar legibilidad para pasar peso. Origen local en evidencia excluida, catálogo público solo proveedor/tool conocida, hash, fuente de declaración. No contenido factual sintético ni números/mapa real. _posts exclusivo E1. Generar también hero responsive 800x450 <=100000bytes; roles diferenciados hero_mobile vs teaser para no usar crop de tarjeta como hero. La transparencia admite unknown sin obligar recreación y sin caption 'Hecho con IA' cuando ese origen no se conoce.

## Allowlist de escritura

- `assets/images/heroes-v2/**`
- `_data/visuales/*.yml`
- `scripts/build_editorial_image_variants.py`
- `docs/plans/20260912-heroes-casen/evidence/images/**`

## Entregables

- per-topic selected hero variants
- visual manifest provenance and image hashes

## Aceptación

- Aceptación final espera: U1.
- AC-H1: `ruby scripts/verify_visual_assets.rb --strict` (cwd: blog_repo). Inventory-complete manifests; formats/dimensions/paths/weight checks; image visual gate recorded per topic.

## Entrega de regreso

- Diff y lista exacta de archivos propios; no adjuntar filas de microdatos.
- Hashes de inputs/código/output y ruta de evidencia, con stdout/exit-code de pruebas.
- Fallos observados y recuperación; lo no ejecutado se declara pending/not_run.
- Hallazgos por severidad, evidencia favorable y contraria; no usar consenso como prueba.
- TODO_STATE de esta tarea, bloqueos y condición concreta de desbloqueo.
- Rollback: Revert only task-owned unpublished diff, preserving source data and unrelated edits; root resolves overlap.
