# Briefing V1: implementer

Estado: pendiente; **no despachar hasta satisfacer dependencias**.

HANDOFF-TRACE: Codex root -> figure_worker (provider: OpenAI; model: heredado salvo ImageGen integrado) via native agent/tool
Modelo solicitado y modelo observado se registran por separado; self_report no equivale a recibo del proveedor.

## Contrato

- Fuente de verdad: ../contract.json, tarea V1. Si este briefing difiere, actualizar desde contrato antes de despacho.
- Dependencias: D2, V0.
- Propietario: figure_worker; proveedor OpenAI.
- Skills a leer al iniciar: data-science-rsh, codigo-multilenguaje, estadistica, agent-browser.
- No estás solo en el workspace: no reviertas cambios ajenos, no edites fuera de allowlist y no hagas staging/commit/push.
- No publiques, envíes mensajes externos, instales dependencias ni exportes microdatos/identificadores/secretos.
- Si necesitas otro archivo, enviar propuesta a root antes de tocarlo; root tiene autoridad de integración.
- Todos los datos originales son de solo lectura; el prefijo @analysis remite al repo analítico según contrato, otros paths al blog.
- Para proveedores externos este briefing es **local**: root genera un packet público/relativo que excluye rutas internas y microdatos; no enviar este paquete entero.

## Entradas

- frozen fiscal aggregates
- D1 approved aggregate outputs
- existing chart theme tokens

## Responsabilidad

Continuación del figure_worker después de V0 y revisión D2. Solo figura CASEN y su test; el estilo común ya existe, cambios adicionales se proponen al root. Nacional+16 regiones; Valpo/Viña panel exploratorio. Tabla HTML la integra E1. Null no es cero; IC singletons ausente marcado. No ImageGen, no hashes impuestos de cifras preliminares.

## Allowlist de escritura

- `scripts/catastro_sii/render_casen_shared_site.py`
- `assets/images/avaluos-ii/casen-shared-site-*`
- `tests/catastro_sii/test_casen_shared_site_figure.py`

## Entregables

- ES/EN light/dark SVG PNG chart set
- chart data table and provenance

## Aceptación

- AC-V1: `/opt/entornos/mamba312/bin/python -m unittest discover -s tests/catastro_sii -p test_casen_shared_site_figure.py -v` (cwd: blog_repo). Validate against source rows not drawing functions; invalid nested values fixture fails; unchanged source aggregate hashes. La prueba debe implementarse en esta tarea; no existe como garantía actual.

## Entrega de regreso

- Diff y lista exacta de archivos propios; no adjuntar filas de microdatos.
- Hashes de inputs/código/output y ruta de evidencia, con stdout/exit-code de pruebas.
- Fallos observados y recuperación; lo no ejecutado se declara pending/not_run.
- Hallazgos por severidad, evidencia favorable y contraria; no usar consenso como prueba.
- TODO_STATE de esta tarea, bloqueos y condición concreta de desbloqueo.
- Rollback: Revert only task-owned unpublished diff, preserving source data and unrelated edits; root resolves overlap.
