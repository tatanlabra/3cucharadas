#!/usr/bin/env bash
#
# Los commits locales no prueban que la publicación esté disponible. Este hook
# se conserva como punto de compatibilidad, pero termina en silencio. El aviso
# Telegram se hace explícitamente con notify_telegram_publication.py, después
# de sus gates de build, remoto, CI y página pública.
set -euo pipefail

exit 0
